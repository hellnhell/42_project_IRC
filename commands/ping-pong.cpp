#include "../server.hpp"



void    Server::checkPing(double dtime)
{
    std::vector<double> t;
    int  i = 0;
    if (dtime)
        t.push_back(dtime);

}

// void    Server::ping_cmmd(std::vector<std::string> const &tokens, User *usr)
// {

// }